<table>
  <th>Name</th>
  <th>Quantity</th>
  <% @cart_items.each do |cart_item| %>
    <tr>
      <td class="name"><%= cart_item.name %></td>
      <td class="quantity"><%= cart_item.quantity %></td>
    </tr>
  <% end %>
</table>
